"""Media generation utilities for Robot Framework trace viewer.

This sub-package provides tools for converting trace screenshot sequences into
shareable media formats: animated GIFs and self-contained HTML slideshows.

Optional dependency:
    Pillow>=9.0 is required for GIF generation (``generate_gif``).
    The HTML slideshow (``generate_slideshow``) uses only the standard library.
"""
